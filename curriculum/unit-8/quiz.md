---
id: unit-8-quiz
title: Module 8 Quiz
blurb: Test what you learned about combining functions with lists and booleans.
---

```json
[
  {
    "id": "unit-8-quiz-1",
    "type": "mcq",
    "prompt": "Which built-in function adds up all the numbers in a list?",
    "options": ["sum()", "max()", "len()", "sorted()"],
    "answer": "sum()",
    "explanation": "sum() adds up all the numbers in a list."
  },
  {
    "id": "unit-8-quiz-2",
    "type": "mcq",
    "prompt": "A function that returns True or False is often named starting with what?",
    "options": ["is_ or has_", "get_ or set_", "do_ or run_", "make_ or build_"],
    "answer": "is_ or has_",
    "explanation": "Names like is_even or has_ticket make a boolean function's purpose obvious."
  },
  {
    "id": "unit-8-quiz-3",
    "type": "fill_blank",
    "prompt": "Fill in the built-in function that finds the largest value: ___(numbers)",
    "answer": "max",
    "explanation": "max(numbers) returns the largest value in the list."
  },
  {
    "id": "unit-8-quiz-4",
    "type": "mcq",
    "prompt": "What does sorted(my_list) do?",
    "options": ["Returns a new sorted list, leaving the original unchanged", "Sorts the original list in place", "Removes duplicates", "Reverses the list"],
    "answer": "Returns a new sorted list, leaving the original unchanged",
    "explanation": "sorted() gives you back a new list; the original list is untouched."
  },
  {
    "id": "unit-8-quiz-5",
    "type": "mcq",
    "prompt": "Why pass a list into a function instead of writing separate code for each list?",
    "options": ["So you can reuse the same logic on different lists", "Because Python requires it", "It makes the code run faster", "Lists can't be used outside functions"],
    "answer": "So you can reuse the same logic on different lists",
    "explanation": "Writing the logic once as a function means you can apply it to any list you pass in."
  },
  {
    "id": "unit-8-quiz-6",
    "type": "mcq",
    "prompt": "What does len(my_list) return?",
    "options": ["The number of items in the list", "The sum of the items", "The largest item", "The list reversed"],
    "answer": "The number of items in the list",
    "explanation": "len() counts how many items are in the list."
  }
]
```
